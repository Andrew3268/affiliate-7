<script>
  $(document).ready(function() {
  $('.menu-trigger').click(function() {
    $('.dealzon_nav ul').slideToggle(500);
  });//end slide toggle
  
  $(window).resize(function() {   
    if (  $(window).width() > 500 ) {     
      $('.dealzon_nav ul').removeAttr('style');
     }
  });//end resize
});//end ready
</script>

<script>
var slideIndex = 0, timeout = 10000;
var slides = document.getElementsByClassName("slide");
var dots = document.getElementsByClassName("dot");
function getSlide(index) {
    slides[slideIndex].classList.remove("index_active");
    dots[slideIndex].classList.remove("activedot");
    slideIndex = index;
    slides[slideIndex].classList.add("index_active");
    dots[slideIndex].classList.add("activedot");
}
showSlides();
function showSlides() {
    slides[slideIndex].classList.remove("index_active");
    dots[slideIndex].classList.remove("activedot");
    slideIndex++;
    if (slideIndex > slides.length - 1) {
        slideIndex = 0;
    }
    ;
    slides[slideIndex].classList.add("index_active");
    dots[slideIndex].classList.add("activedot");
    setTimeout(showSlides, timeout); // Change image every 2 seconds
}
</script>